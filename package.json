{
  "name": "@aiiq/mcp",
  "version": "0.1.1",
  "mcpName": "io.github.shea256/aiiq-mcp",
  "description": "Model Context Protocol server for AI IQ (aiiq.org) — query model IQ, rankings, benchmarks, and methodology.",
  "type": "module",
  "bin": { "aiiq-mcp": "dist/index.js" },
  "files": ["dist"],
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "prepublishOnly": "pnpm build"
  },
  "keywords": ["mcp", "model-context-protocol", "ai", "aiiq", "benchmarks", "llm"],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "@types/node": "^20.0.0"
  },
  "publishConfig": { "access": "public" },
  "license": "MIT"
}
